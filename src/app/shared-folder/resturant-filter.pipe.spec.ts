import { ResturantFilterPipe } from './resturant-filter.pipe';

describe('ResturantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResturantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
